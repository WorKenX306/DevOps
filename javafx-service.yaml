apiVersion: v1
kind: Service
metadata:
  name: javafx-service
  labels:
    app: javafx-app
spec:
  selector:
    app: javafx-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
